# -------- InfluxDB Configuration --------
# Admin credentials (used for initial setup)
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=changeme_influxdb_admin_password

# Organization and storage settings
INFLUXDB_ORG=my-org
INFLUXDB_BUCKET=iot_data

# Admin API token (for service authentication)
# Generate a strong token before deploying to production
INFLUXDB_TOKEN=change-me-to-a-strong-secret-token-at-least-32-chars

# -------- Analytics Service Configuration --------
HTTP_ADDR=:3000
MQTT_SENSOR_TOPIC=sensors/mpu6050
SENSOR_DEVICE_ID=ESP32_MPU6050

# hardware/sensor/sensor.ino publishes timestamp, roll, and pitch only.
# Cloud maps those angles to posture.
# A sample is classified as bad when abs(roll) or abs(pitch) reaches these thresholds.
POSTURE_BAD_ROLL_DEG=20
POSTURE_BAD_PITCH_DEG=20
INFLUX_MEASUREMENT=raw_mpu6050
